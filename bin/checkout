#!/usr/bin/env ruby

rggen_root = ENV['RGGEN_ROOT'] || File.join('../../', __dir__)

require File.join(
  rggen_root, 'rggen-checkout', 'lib', 'rggen', 'checkout'
)

RgGen::Checkout.checkout(rggen_root)
